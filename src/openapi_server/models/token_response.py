# coding: utf-8

"""
    OAuth 2.0 + OpenID Connect — RFC-Canonical OpenAPI Specification

    # OAuth 2.0 + OpenID Connect — RFC-Canonical OpenAPI  This specification describes the HTTP surface of an OAuth 2.0 / OpenID Connect authorization server in a vendor-neutral way. Every endpoint and field is cross-referenced to the source RFC or specification section.  ## Covered specifications  | Spec | Year | Subject | |---|---|---| | RFC 6749 | 2012 | OAuth 2.0 Authorization Framework | | RFC 6750 | 2012 | Bearer Token Usage | | RFC 7009 | 2013 | Token Revocation | | RFC 7517 | 2015 | JSON Web Key (JWK / JWKS) | | RFC 7519 | 2015 | JSON Web Token (JWT) | | RFC 7636 | 2015 | PKCE — Proof Key for Code Exchange | | RFC 7662 | 2015 | Token Introspection | | RFC 8414 | 2018 | Authorization Server Metadata | | RFC 8628 | 2019 | Device Authorization Grant | | RFC 8693 | 2020 | Token Exchange | | RFC 9068 | 2021 | JWT Profile for OAuth 2.0 Access Tokens | | RFC 9126 | 2021 | Pushed Authorization Requests (PAR) | | RFC 9396 | 2023 | Rich Authorization Requests (RAR) | | RFC 9449 | 2023 | DPoP — Demonstrating Proof of Possession | | RFC 9700 | 2025 | OAuth 2.0 Security Best Current Practice | | OAuth 2.1 (draft) | ongoing | Consolidated modern best practices | | OIDC Core 1.0 | 2014 | OpenID Connect identity layer | | OIDC Discovery 1.0 | 2014 | `.well-known/openid-configuration` schema |  ## Modeling notes  - **`/authorize` is browser-driven.** OpenAPI cannot precisely model 302 redirects   to a client-controlled `redirect_uri` followed by HTML consent. The parameters   and redirect semantics are described, but the response body is an approximation.   The exact behavior is in RFC 6749 §4.1.2.  - **`/token` uses a `grant_type` discriminator.** Per RFC 6749 §3.2, all grants   POST to one endpoint and are dispatched by the `grant_type` form field. This   spec uses `oneOf` + discriminator. Tooling without discriminator support may   need to flatten the schema.  - **OAuth 2.1 defaults are applied.** PKCE is mandatory for all public clients;   Implicit (`response_type=token`) and Resource Owner Password (`grant_type=password`)   grants are excluded as they are deprecated by OAuth 2.1.  - **DPoP is supported via a request header.** The `DPoP` header is documented on   relevant endpoints; the proof JWT itself is not modeled as a body schema. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json




from pydantic import ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from openapi_server.models.authorization_detail import AuthorizationDetail
from openapi_server.models.object import object
from openapi_server.models.token_type_uri import TokenTypeURI
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class TokenResponse(object):
    """
    Successful token response — RFC 6749 §5.1, with extensions: OIDC Core §3.1.3.3 adds `id_token`; RFC 8693 §2.2.1 adds `issued_token_type` (REQUIRED for token-exchange responses); RFC 9396 §6 adds `authorization_details`. Returned with `Cache-Control: no-store` and `Pragma: no-cache` headers. 
    """ # noqa: E501
    access_token: StrictStr = Field(description="The access token issued. Format is implementation-defined: an opaque string (validated via introspection) or a JWT (RFC 9068 — see `JWTAccessTokenClaims` for the decoded shape). ")
    token_type: StrictStr = Field(description="Token presentation scheme. `Bearer` (RFC 6750) for unbound tokens, `DPoP` (RFC 9449) for sender-constrained tokens, or `N_A` for token-exchange responses where no usable access token is returned (RFC 8693 §2.2.1). ")
    expires_in: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Lifetime of the access token in seconds.")
    refresh_token: Optional[StrictStr] = Field(default=None, description="Optional refresh token (RFC 6749 §5.1). Per OAuth 2.1 §4.3.1 and RFC 9700 §2.2.2, refresh tokens issued to public clients SHOULD be sender-constrained (DPoP) or rotated on every use. ")
    scope: Optional[StrictStr] = Field(default=None, description="Granted scope, if different from what was requested.")
    id_token: Optional[StrictStr] = Field(default=None, description="OIDC: signed JWT identifying the authenticated user. The decoded payload conforms to `IDTokenClaims`. Issued only when the request scope contained `openid`. ")
    issued_token_type: Optional[TokenTypeURI] = Field(default=None, description="REQUIRED in RFC 8693 §2.2.1 token-exchange responses; absent from other token responses. ")
    authorization_details: Optional[List[AuthorizationDetail]] = Field(default=None, description="RAR (RFC 9396 §6): granted authorization detail objects. MAY be a subset of those requested. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["access_token", "token_type", "expires_in", "refresh_token", "scope", "id_token", "issued_token_type", "authorization_details"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of TokenResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
                "additional_properties",
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in authorization_details (list)
        _items = []
        if self.authorization_details:
            for _item in self.authorization_details:
                if _item:
                    _items.append(_item.to_dict())
            _dict['authorization_details'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of TokenResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "access_token": obj.get("access_token"),
            "token_type": obj.get("token_type"),
            "expires_in": obj.get("expires_in"),
            "refresh_token": obj.get("refresh_token"),
            "scope": obj.get("scope"),
            "id_token": obj.get("id_token"),
            "issued_token_type": obj.get("issued_token_type"),
            "authorization_details": [AuthorizationDetail.from_dict(_item) for _item in obj.get("authorization_details")] if obj.get("authorization_details") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


