# coding: utf-8

"""
    OAuth 2.0 + OpenID Connect — RFC-Canonical OpenAPI Specification

    # OAuth 2.0 + OpenID Connect — RFC-Canonical OpenAPI  This specification describes the HTTP surface of an OAuth 2.0 / OpenID Connect authorization server in a vendor-neutral way. Every endpoint and field is cross-referenced to the source RFC or specification section.  ## Covered specifications  | Spec | Year | Subject | |---|---|---| | RFC 6749 | 2012 | OAuth 2.0 Authorization Framework | | RFC 6750 | 2012 | Bearer Token Usage | | RFC 7009 | 2013 | Token Revocation | | RFC 7517 | 2015 | JSON Web Key (JWK / JWKS) | | RFC 7519 | 2015 | JSON Web Token (JWT) | | RFC 7636 | 2015 | PKCE — Proof Key for Code Exchange | | RFC 7662 | 2015 | Token Introspection | | RFC 8414 | 2018 | Authorization Server Metadata | | RFC 8628 | 2019 | Device Authorization Grant | | RFC 8693 | 2020 | Token Exchange | | RFC 9068 | 2021 | JWT Profile for OAuth 2.0 Access Tokens | | RFC 9126 | 2021 | Pushed Authorization Requests (PAR) | | RFC 9396 | 2023 | Rich Authorization Requests (RAR) | | RFC 9449 | 2023 | DPoP — Demonstrating Proof of Possession | | RFC 9700 | 2025 | OAuth 2.0 Security Best Current Practice | | OAuth 2.1 (draft) | ongoing | Consolidated modern best practices | | OIDC Core 1.0 | 2014 | OpenID Connect identity layer | | OIDC Discovery 1.0 | 2014 | `.well-known/openid-configuration` schema |  ## Modeling notes  - **`/authorize` is browser-driven.** OpenAPI cannot precisely model 302 redirects   to a client-controlled `redirect_uri` followed by HTML consent. The parameters   and redirect semantics are described, but the response body is an approximation.   The exact behavior is in RFC 6749 §4.1.2.  - **`/token` uses a `grant_type` discriminator.** Per RFC 6749 §3.2, all grants   POST to one endpoint and are dispatched by the `grant_type` form field. This   spec uses `oneOf` + discriminator. Tooling without discriminator support may   need to flatten the schema.  - **OAuth 2.1 defaults are applied.** PKCE is mandatory for all public clients;   Implicit (`response_type=token`) and Resource Owner Password (`grant_type=password`)   grants are excluded as they are deprecated by OAuth 2.1.  - **DPoP is supported via a request header.** The `DPoP` header is documented on   relevant endpoints; the proof JWT itself is not modeled as a body schema. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json




from pydantic import ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_server.models.object import object
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class JWK(object):
    """
    JSON Web Key per RFC 7517 §4. Algorithm-specific parameters are defined in RFC 7518 (JWA) and RFC 8037 (OKP). The `kty` enum is kept open per the IANA registry — additional key types (e.g. post-quantum) may appear over time. 
    """ # noqa: E501
    kty: StrictStr = Field(description="Key Type (RFC 7517 §4.1) — IANA-registered.")
    use: Optional[StrictStr] = Field(default=None, description="Public Key Use (§4.2). `sig` for signature, `enc` for encryption. MUST be disjoint with `key_ops`. ")
    key_ops: Optional[List[StrictStr]] = Field(default=None, description="Allowed key operations (§4.3). Values are from the IANA JSON Web Key Operations registry (§8.3): `sign`, `verify`, `encrypt`, `decrypt`, `wrapKey`, `unwrapKey`, `deriveKey`, `deriveBits`. MUST be disjoint with `use`. ")
    alg: Optional[StrictStr] = Field(default=None, description="Algorithm intended for use with the key (§4.4).")
    kid: Optional[StrictStr] = Field(default=None, description="Key ID for selection in a JWKS (§4.5).")
    x5u: Optional[StrictStr] = Field(default=None, description="URL of an X.509 certificate or chain (§4.6).")
    x5c: Optional[List[StrictStr]] = Field(default=None, description="X.509 certificate chain (§4.7). Each item is a **base64-encoded** (NOT base64url-encoded) DER PKIX certificate. The first entry is the certificate corresponding to the public key. ")
    x5t: Optional[StrictStr] = Field(default=None, description="X.509 SHA-1 thumbprint (§4.8); base64url-encoded.")
    x5t_s256: Optional[StrictStr] = Field(default=None, description="X.509 SHA-256 thumbprint (§4.9); base64url-encoded.", alias="x5t#S256")
    n: Optional[StrictStr] = Field(default=None, description="RSA modulus, base64url-encoded.")
    e: Optional[StrictStr] = Field(default=None, description="RSA exponent, base64url-encoded.")
    crv: Optional[StrictStr] = Field(default=None, description="Curve identifier. EC: `P-256`, `P-384`, `P-521`, `secp256k1`. OKP (RFC 8037): `Ed25519`, `Ed448`, `X25519`, `X448`. ")
    x: Optional[StrictStr] = Field(default=None, description="EC/OKP x coordinate or public key, base64url.")
    y: Optional[StrictStr] = Field(default=None, description="EC y coordinate, base64url. Not used for OKP.")
    k: Optional[StrictStr] = Field(default=None, description="Symmetric key value (oct kty), base64url-encoded.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["kty", "use", "key_ops", "alg", "kid", "x5u", "x5c", "x5t", "x5t#S256", "n", "e", "crv", "x", "y", "k"]

    @field_validator('use')
    def use_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('sig', 'enc',):
            raise ValueError("must be one of enum values ('sig', 'enc')")
        return value

    @field_validator('key_ops')
    def key_ops_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in ('sign', 'verify', 'encrypt', 'decrypt', 'wrapKey', 'unwrapKey', 'deriveKey', 'deriveBits',):
                raise ValueError("each list item must be one of ('sign', 'verify', 'encrypt', 'decrypt', 'wrapKey', 'unwrapKey', 'deriveKey', 'deriveBits')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of JWK from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
                "additional_properties",
            },
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of JWK from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "kty": obj.get("kty"),
            "use": obj.get("use"),
            "key_ops": obj.get("key_ops"),
            "alg": obj.get("alg"),
            "kid": obj.get("kid"),
            "x5u": obj.get("x5u"),
            "x5c": obj.get("x5c"),
            "x5t": obj.get("x5t"),
            "x5t#S256": obj.get("x5t#S256"),
            "n": obj.get("n"),
            "e": obj.get("e"),
            "crv": obj.get("crv"),
            "x": obj.get("x"),
            "y": obj.get("y"),
            "k": obj.get("k")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


